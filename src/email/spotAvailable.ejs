<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Spot Available for Event</title>
    <style>
      body {
        font-family: "Arial", sans-serif;
        background-color: #f3f4f6;
        margin: 0;
        padding: 0;
        color: #333;
      }

      .container {
        max-width: 600px;
        margin: 50px auto;
        padding: 20px;
      }

      .header {
        background-color: #4f46e5;
        padding: 20px;
        text-align: center;
        border-top-left-radius: 10px;
        border-top-right-radius: 10px;
        color: white;
      }

      .header h1 {
        margin: 0;
        font-size: 24px;
      }

      .content {
        background-color: white;
        padding: 30px;
        border-bottom-left-radius: 10px;
        border-bottom-right-radius: 10px;
        box-shadow: 0 8px 20px rgba(0, 0, 0, 0.05);
      }

      .content p {
        line-height: 1.6;
        font-size: 16px;
        margin-bottom: 15px;
      }

      .content strong {
        font-size: 18px;
        color: #4f46e5;
      }

      .cta-button {
        display: inline-block;
        margin: 20px 0;
        padding: 15px 25px;
        background: linear-gradient(90deg, #6366f1, #2563eb);
        color: white;
        text-decoration: none;
        font-size: 16px;
        border-radius: 25px;
        text-align: center;
        font-weight: bold;
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
        transition: all 0.3s ease-in-out;
      }

      .cta-button:hover {
        transform: scale(1.05);
        box-shadow: 0 6px 20px rgba(0, 0, 0, 0.3);
      }

      .footer {
        text-align: center;
        margin-top: 20px;
        font-size: 14px;
        color: #6b7280;
      }

      .footer a {
        color: #1d4ed8;
        text-decoration: none;
        font-weight: bold;
      }

      .divider {
        height: 1px;
        background-color: #e5e7eb;
        margin: 20px 0;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="header">
        <h1>You're Off the Waitlist!</h1>
      </div>
      <div class="content">
        <p>Hi <%= user.name %>,</p>
        <p>
          A spot has become available for the event "<strong
            ><%= event.title %></strong
          >".
        </p>
        <div class="divider"></div>
        <p>
          <strong>Event Details:</strong><br />
          <strong>Date:</strong> <%= new Date(event.date).toLocaleDateString()
          %><br />
          <strong>Time:</strong> <%= event.time %><br />
          <strong>Location:</strong> <%= event.location %>
        </p>
        <p>
          You have been moved from the waitlist to reserved status. We look
          forward to seeing you at the event!
        </p>
        <a href="<%= event.detailsLink %>" class="cta-button"
          >View Event Details</a
        >
      </div>
      <div class="footer">
        <p>Best regards,</p>
        <p>Your App Team</p>
        <p>
          Need help? <a href="mailto:support@yourapp.com">Contact Support</a>
        </p>
      </div>
    </div>
  </body>
</html>
